/* ========================================
	DX22Base/
	------------------------------------
	線描画用ピクセルシェーダー
	------------------------------------
	説明：線(グリッド線や、レイ等)を描画するためのピクセルシェーダー
	------------------------------------
	PS_Line.hlsl
========================================== */

// ピクセルシェーダー入力構造体
struct PS_IN
{
    float4 pos : SV_POSITION;
    float4 color : COLOR0;
};

// メイン関数
float4 main(PS_IN pin) : SV_TARGET0
{
	// 頂点カラーをそのまま出力
    return pin.color;
}